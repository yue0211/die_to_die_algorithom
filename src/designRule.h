#pragma once
#include <iostream>
#include <string>

struct DesignRule {
    int via_opening_diameter;
    int via_pad_diameter;
    int minimum_via_pad_spacing; 
    int minimum_via_spacing;
    int minimum_line_width;
    int minimum_line_spacing; 
    int minimum_line_via_spacing; 
};